###############################################
# Requisitos para entorno Raspberry Pi (hardware real)
# Usa Python 3.11 o 3.12 recomendado (3.13 aún rompe algunos wheels)
# Instala cabeceras del sistema antes:
#   sudo apt update && sudo apt install -y python3-dev python3-venv build-essential libffi-dev
#
# Si NO estás en una Raspberry Pi o en CI, usa requirements-base.txt únicamente.
###############################################

-r requirements-base.txt

# Paquetes hardware (solo en Raspberry Pi)
adafruit-circuitpython-dht
Adafruit-Blinka
RPi.GPIO

# Nota: Si falla RPi.GPIO o sysv_ipc en instalación fuera de Raspberry Pi,
# ejecuta: pip install -r requirements-base.txt  (sin estos paquetes)
